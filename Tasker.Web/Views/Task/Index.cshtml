@model Tasker.Data.Manager.PagedList<Tasker.Web.Models.TaskViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().DateEnd)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateEnd)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
            </td>
        </tr>
    }
</table>

<table>
    <tr>
        @if (Model.CurrentIndexes.First() != 1)
        {
            <td style="padding: 5px;">
                @Html.ActionLink("1", "Index", new { page = 1 })
            </td>
        }

        @if (Model.CurrentIndexes.First() > 2)
        {
            <td>....</td>
        }

        @foreach (var i in Model.CurrentIndexes)
        {
            var style = Model.PageNumber == i ? "font-weight: bold;" : "font-weight: normal;";
            <td style="@style; padding: 5px;">
                @Html.ActionLink(i.ToString(), "Index", new { page = i })
            </td>
        }

        @if (Model.CurrentIndexes.Last() < Model.PageCount - 1)
        {
            <td>....</td>
        }

        @if (Model.CurrentIndexes.Last() != Model.PageCount)
        {
            <td style="padding: 5px;">
                @Html.ActionLink(Model.PageCount.ToString(), "Index", new { page = Model.PageCount })
            </td>
        }
    </tr>
</table>